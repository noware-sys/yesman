#! /bin/bash

# If required,
# run commands from the in-between versions.
#yesman-pre-inter "${repo}" "${pkg_name}" "${ver_dest}" "${ver_src}" "${arch}" '*';
# Or,
# a specific version.
#"${yesman_cfg_hookdir}/""${repo}-${pkg_name}-1.5.9-1-${arch}";

# Run the precommands specific for this version.
#rm -rf /usr/lib/libzita-resampler.so.1;
echo "${0}";
exit 2;
